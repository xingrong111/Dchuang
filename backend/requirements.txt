# ============================================================
# 智绘锡承 - 后端 Python 依赖
# 项目: 基于多模态的非遗3D模型生成平台
# 开发分支: B_houduan
#
# 版本说明:
# - 项目开发规范文档 v1.0 要求 Python 3.9 - 3.11
# - 本机实际开发环境为 Python 3.13.14（高于规范要求，向下兼容）
# - 以下版本范围均兼容 Python 3.9+，请勿使用过旧版本
# ============================================================

# --- Web 框架 ---
Flask>=3.0.0,<4.0.0

# --- ORM 与数据库 ---
# SQLAlchemy 2.x + Flask-SQLAlchemy 3.x（规范文档 4.1.1）
Flask-SQLAlchemy>=3.1.0,<4.0.0
# 数据库迁移（规范文档 4.1.1: flask db migrate / upgrade）
Flask-Migrate>=4.0.0,<5.0.0
# MySQL 驱动（规范文档 4.1.2: mysql+pymysql）
PyMySQL>=1.1.0,<2.0.0

# --- 跨域 ---
# 规范文档 4.1.1: flask_cors
Flask-CORS>=4.0.0,<7.0.0

# --- 配置加载 ---
# 读取 .env 文件（敏感配置全部来自环境变量）
python-dotenv>=1.0.0,<2.0.0

# --- 密码哈希（Werkzeug 随 Flask 安装，此处显式声明） ---
Werkzeug>=3.0.0,<4.0.0

# --- 后续阶段预留（本次不安装，仅注释备忘） ---
# JWT 认证（阶段3）: flask-jwt-extended>=4.6.0
# Redis 缓存（阶段3+）: redis>=5.0.0
# 限流（规范文档 4.1.2 RATELIMIT）: Flask-Limiter>=3.5.0
# 腾讯混元 SDK（阶段7）: tencentcloud-sdk-python
# 测试（规范文档 第五章）: pytest>=8.0.0
